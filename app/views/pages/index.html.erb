<h1>トップページ</h1>

<% if user_signed_in? %>
<div class="btn-group" data-toggle="buttons">
	<label class="btn btn-success active">
		<input type="radio" autocomplete="off" checked> 電源ON
	</label>
	<label class="btn btn-success">
		<input type="radio" autocomplete="off"> 電源OFF
	</label>
</div>
<% else %>
こんにちは
<% end %>
